default:
    image: OpenJDK-15.0.2

test-job:
    stage: test
    script:
        - echo "JAVA_HOME=${JAVA_HOME}"
build-job:
    stage: build
    script:
        - ./gradlew assemble
        - echo "Assemble .."
deploy-job:
    stage: deploy
    script:
        - echo "Deploy"
        - ./gradlew test
